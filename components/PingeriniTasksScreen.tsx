import React, {FunctionComponent} from 'react';
import {PingeriniToDoList} from './PingeriniToDoList';

export const PingeriniTasksScreen: FunctionComponent = props => (
    <PingeriniToDoList {...props} />
);
